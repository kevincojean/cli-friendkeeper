#!/usr/bin/env bash
set -euo pipefail
REAL_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
PYTHONPATH="$REAL_DIR/src/python/main"
export PYTHONPATH
exec uv run --project "$REAL_DIR" python -m cli_friendkeeper.ccli.ccli "$@"
